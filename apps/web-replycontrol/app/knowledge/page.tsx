import { PlatformPage } from "../components/PlatformPage";
const knowledge=[["01","Sources","Bring approved documents, records and operational knowledge into one governed context layer."],["02","Retrieval","Find relevant context at the moment a task needs it."],["03","Memory","Preserve durable project knowledge without confusing it with transient conversation state."],["04","Grounding","Show where important answers and decisions came from."],["05","Refresh","Keep knowledge current through deliberate update paths and schema checks."],["06","Boundaries","Separate protected information from data that can safely enter downstream workflows."]];
export default function KnowledgeRoute(){return <PlatformPage eyebrow="KNOWLEDGE / CONTEXT" title={<>Useful memory.<br/><em>Controlled context.</em></>} lead="A context layer for retrieval, grounding, durable knowledge and explicit data boundaries."><section className="mc-section"><div className="mc-section-head"><span>KNOWLEDGE LAYER</span><h2>Make context available without making it invisible.</h2></div><div className="mc-rail">{knowledge.map(([n,t,d])=><article className="mc-module" key={n}><span className="num">{n}</span><h3>{t}</h3><p>{d}</p></article>)}</div></section><section className="mc-section"><div className="mc-section-head"><span>CONTEXT FLOW</span><h2>Ingest → index → retrieve → ground → record.</h2></div><div className="mc-substrate"><div><code>INGEST</code><h3>Normalize inputs</h3><p>Convert approved sources into structured, searchable material.</p></div><div><code>INDEX</code><h3>Organize retrieval</h3><p>Keep source metadata and retrieval behavior explicit.</p></div><div><code>GROUND</code><h3>Support the task</h3><p>Place useful context next to the objective rather than behind a separate product.</p></div><div><code>RECORD</code><h3>Preserve provenance</h3><p>Retain the relationship between the source, the workflow and the resulting action.</p></div></div></section></PlatformPage>}
