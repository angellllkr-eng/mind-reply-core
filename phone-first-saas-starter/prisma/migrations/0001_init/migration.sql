-- Prototype migration placeholder.
-- Run `pnpm prisma:migrate` locally to generate a database-specific migration
-- after reviewing prisma/schema.prisma.

CREATE TABLE IF NOT EXISTS "_prototype_migration_marker" (
  "id" TEXT PRIMARY KEY
);
